# Git 与发布规则

## 工作区完整性

- 编辑前后分别检查 `git status --short` 和相关 `git diff`。
- npm 与 PyPI 的安装和发布默认使用官方上游源；临时切换第三方镜像仅限本地诊断，任务结束前恢复，且不得写入包管理配置、锁文件、CI、文档或提交。提交或发布前检查实际生效的源和锁文件来源。
- 不提交运行日志、临时测试目录、缓存、构建产物、用户备份、真实数据或本机绝对路径。
- `tests/`、`web-vue/tests/` 与测试脚本只用于本地验证，不纳入 Git 跟踪或提交；提交中不得夹带测试源码变更。
- 生产代码导入的新增文件必须被纳入提交；不得留下只在当前脏工作区可运行的提交。
- 提交前运行 `git diff --check`，并确认删除、重命名和未跟踪文件都符合意图。
- 工作区过大时按行为和依赖关系拆分提交，但不能把一个不可运行的中间状态伪装成独立提交。

## 提交前暂存清单

创建提交前必须执行以下检查，不能只根据文件数量或 `git status` 判断提交是否干净：

1. 运行 `git status --short`，区分已暂存、未暂存和未跟踪内容。
2. 运行 `git diff --cached --name-status`，逐个确认暂存文件的用途、所属行为及其进入本次提交的必要性。
3. 检查 `git diff --cached`，确认提交内部可运行，没有遗漏生产代码引用的新增文件，也没有夹带无关修改。
4. 运行 `git diff --cached --check`，再核对删除、重命名、子模块或本地依赖变化是否符合授权。
5. 暂存区出现测试源码、缓存、构建产物、本机路径、用户数据或无法解释用途的文件时，停止提交并先移出暂存区或向用户说明。

调整忽略规则时必须同时核对 `.gitignore` 和 Git 索引：使用 `git check-ignore -v <path>` 确认忽略来源，并使用 `git ls-files <path>` 检查是否仍被跟踪。`.gitignore` 不会自动移除已跟踪文件；规则、忽略配置和实际索引状态不一致时，不得声称收口完成。

## 提交范围

- `D:\chatgpt2api` 与 `D:\nanocat` 是两个独立仓库，必须分别查看差异、测试和提交。
- 用户只说“提交”时，不自动包含另一个仓库；汇报中明确每个仓库是否有未提交或未推送内容。
- 不修改、不压缩、不重写用户已有提交，除非用户明确要求。
- 未经明确授权，不执行 push、tag、GitHub Release、镜像构建/推送或 npm 发布。

## Nanocat 到宿主的发布顺序

1. 宿主开发阶段临时接入 `D:\nanocat`，完成真实页面联调。
2. 在 Nanocat 运行 check、build 和相关 E2E，并独立提交。
3. 由用户验收宿主中的实际效果。
4. 用户明确授权后更新 Nanocat 版本、Changelog，并执行 npm 发布。
5. 确认注册表中的新版本后，宿主切换到正式 npm semver，重新安装依赖。
6. 在宿主运行 runtime 测试和生产构建，再独立提交依赖切换。

不得跳过用户验收直接发布，也不得把 `file:D:\nanocat`、`file:../../nanocat` 或临时 tarball 当作最终发布依赖。

## 版本与文档

- GitHub 发布 workflow 的 push 触发器只监听 `v*` 标签；不得因 `main` 推送启动。`pull_request` 与 `workflow_dispatch` 可以保留用于仅验证，但 Release、镜像与其他发布 job 必须显式限制为 `v*` 标签。
- 用户可感知的功能、Interface 或行为变化更新 `CHANGELOG.md` 的 Unreleased；纯内部重构无需逐条记录。
- 同一版本的 `CHANGELOG.md` 条目必须按 `[新增]`、`[优化]`、`[修复]` 的顺序分组，同类条目保持连续，不得交叉排列。
- 当前文档只描述已验证行为。删除路由或功能时，同一修改删除对应的 current 文档内容。
- 计划文件不是发布记录；过期计划应删除或明确标为 historical，不能继续挂在 current 文档索引中。
- 版本号、tag、npm 包和容器标签必须指向同一已验证提交；未明确发布时不提升版本。
